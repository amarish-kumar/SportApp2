using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SportApp2.Infrastructure.Extensions
{
    public static class PropertyExtenstions
    {
        //public static string GetNameById(this IEnumerable<object> source, int objectId)
        //{
        //    Type sourceType = source.GetType().GetGenericArguments()[0];
        //    if(sourceType.IsClass && sourceType.GetProperty(sourceType.Name) != null && sourceType.GetProperty(sourceType.Id))
        //    {
        //        var targetProp = sourceType.GetProperty(sourceType.Name);
        //        //var m = source.Select(o => o.Name).ToList();
        //        //return sourceType.GetProperty(sourceType.Name).ToString();
        //        foreach(var sourceProp in sourceType.Id)
        //        {

        //        }
        //    }
        //    else
        //    {
        //        throw new Exception("Object does not have property Name.");
        //    }
        //}
    }
}
